{
  "name": "@vadistic/workspace",
  "private": true,
  "description": "Personal @vadistic configs & utils",
  "homepage": "https://vadistic.netlify.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/vadistic/configs"
  },
  "license": "MIT",
  "author": "Jakub Wadas <vadistic@gmail.com>",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "lerna run format",
    "lint": "lerna run lint",
    "pkg:sync": "tsask sync-pkg && yarn lerna exec 'tsask sync-pkg'",
    "pkg:ncu": "ncu -u && yarn lerna exec 'ncu -u'",
    "prepublishOnly": "yarn pkg:sync && yarn format && yarn lint"
  },
  "dependencies": {},
  "devDependencies": {
    "@vadistic/tsask": "^0.2.2",
    "lerna": "^3.20.2",
    "npm-check-updates": "^4.1.1",
    "prettier": "^2.0.4",
    "typescript": "^3.8.3"
  }
}
