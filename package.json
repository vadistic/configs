{
  "name": "@vadistic/configs",
  "private": true,
  "description": "Personal @vadistic configs",
  "homepage": "https://vadistic.netlify.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/vadistic/workspace"
  },
  "license": "MIT",
  "author": "Jakub Wadas <vadistic@gmail.com>",
  "workspaces": [
    "configs/*"
  ],
  "scripts": {
    "build:all": "lerna run --ignore @vadistic/fixture-monorepo build ",
    "build:pkg": "lerna run --ignore @vadistic/fixture-* build ",
    "clean": "lerna run --ignore @vadistic/fixture-monorepo clean",
    "format": "lerna run --ignore @vadistic/fixture-monorepo format",
    "lint": "lerna run --ignore @vadistic/fixture-monorepo lint",
    "prepublishOnly": "yarn sync && yarn clean && yarn format && yarn lint && yarn build:pkg",
    "sync:dotfiles": "sh scripts/copy-dotfiles.sh",
    "sync:pkg": "lerna exec 'task sync-pkg'",
    "sync": "yarn sync:dotfiles && yarn sync:pkg"
  },
  "dependencies": {},
  "devDependencies": {
    "@vadistic/eslint-config-base": "0.2.0",
    "@vadistic/prettier-config": "0.2.0",
    "@vadistic/tsask": "0.2.0",
    "eslint": "^6.8.0",
    "lerna": "^3.20.2",
    "npm-check-updates": "^4.1.0",
    "prettier": "^2.0.2",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  }
}
