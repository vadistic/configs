/* eslint-disable import/no-extraneous-dependencies */

const common = require('@vadistic/stack-ts-lib/plopfile')

module.exports = (
  /** @type {import('plop').NodePlopAPI} */
  plop,
) => {
  common(plop)
}
