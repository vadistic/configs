{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "moduleResolution": "node",
    "module": "esnext",

    "target": "esnext",
    "lib": ["dom", "dom.iterable", "esnext"],

    "allowJs": false,
    "jsx": "react",

    // cra does not resolve extend so it needs to be here
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
