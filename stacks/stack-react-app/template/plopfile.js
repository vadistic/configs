/* eslint-disable import/no-extraneous-dependencies */

const common = require('@vadistic/stack-react-app/plopfile')

module.exports = (
  /** @type {import('plop').NodePlopAPI} */
  plop,
) => {
  common(plop)
}
